"""Retrieve-then-compress composition + diagnostics (E3+).

TODO: compose the retrieval and compression stages; measure effective capacity c_eff;
construct and characterize the failure-transfer set T (retrieval OK + gold-compression OK,
pipeline FAILS); relate |T| to the principal angles between the retriever's discriminative
subspace and the compressor's reconstructive subspace. This is the headline (C2).
"""
