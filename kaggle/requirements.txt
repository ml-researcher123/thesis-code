# Core (preinstalled on Kaggle GPU image, listed for local/CPU reproduction)
torch>=2.1
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0

# Added as later stages land (E2 compression, E3+ pipeline). Uncomment on Kaggle
# when the experiment that needs them is queued, to keep startup fast.
# transformers>=4.44
# sentence-transformers>=3.0
# datasets>=2.20
# accelerate>=0.33
# peft>=0.12
# faiss-cpu>=1.8
